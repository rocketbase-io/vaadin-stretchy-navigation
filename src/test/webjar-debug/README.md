This folder exists so you can unzip webjars here for debugging purposes.
The webjar files should end up inside META-INF/resources/webjar/... to be loaded instead of the files inside the actual webjars.
